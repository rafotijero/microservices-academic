# ===========================================
# CONFIGURACIÓN DEL SISTEMA DE MICROSERVICIOS
# ===========================================
# Copiar este archivo como .env y configurar los valores

# ===========================================
# CONFIGURACIÓN DE EMAIL (Notificaciones)
# ===========================================
# Puedes usar Mailtrap, Ethereal, Gmail u otro servicio SMTP
MAIL_HOST=smtp.mailtrap.io
MAIL_PORT=2525
MAIL_USERNAME=6abd9f88309e4d
MAIL_PASSWORD=4ec30b10b54c0b

# ===========================================
# CONFIGURACIÓN JWT (Autenticación)
# ===========================================
# Secreto para firmar tokens (mínimo 32 caracteres)
JWT_SECRET=cambiar_por_secreto_seguro_minimo_32_caracteres
# Expiración del access token en milisegundos (default: 1 hora)
JWT_ACCESS_EXPIRATION=3600000
# Expiración del refresh token en milisegundos (default: 24 horas)
JWT_REFRESH_EXPIRATION=86400000

# ===========================================
# CONFIGURACIÓN DE BASES DE DATOS
# ===========================================
# PostgreSQL - Auth
DB_AUTH_USER=postgres
DB_AUTH_PASSWORD=postgres
DB_AUTH_NAME=auth_db

# PostgreSQL - Management
DB_MANAGEMENT_USER=postgres
DB_MANAGEMENT_PASSWORD=postgres
DB_MANAGEMENT_NAME=academic_db

# PostgreSQL - Audit
DB_AUDIT_USER=postgres
DB_AUDIT_PASSWORD=postgres
DB_AUDIT_NAME=audit_db

# ===========================================
# CONFIGURACIÓN DE RABBITMQ
# ===========================================
RABBITMQ_USER=admin
RABBITMQ_PASSWORD=admin123

# ===========================================
# PUERTOS DE SERVICIOS
# ===========================================
# Modificar si hay conflictos con otros servicios locales
PORT_AUTH=8081
PORT_MANAGEMENT=8082
PORT_NOTIFICATION=8083
PORT_AUDIT=8084
PORT_RABBITMQ=5672
PORT_RABBITMQ_UI=15672
PORT_KAFKA=9092
PORT_KAFKA_EXTERNAL=29092
PORT_KAFKA_UI=8080
PORT_ZOOKEEPER=2181
PORT_POSTGRES_AUTH=5434
PORT_POSTGRES_MANAGEMENT=5433
PORT_POSTGRES_AUDIT=5435
